tags:
  - Contacts
summary: Get contacts
operationId: fetchContacts
description: Get contacts
security:
  - bearerAuth: []
parameters:
  - in: query
    name: page
    description: The page number for pagination (starting from 1).
    required: false
    schema:
      type: integer
      default: 1
      example: 7

  - in: query
    name: perPage
    description: The number of contacts per page.
    required: false
    schema:
      type: integer
      default: 10
      example: 20

  - in: query
    name: name
    description: >
      Sort order for the `name` field.  
      Possible values: `asc` (ascending) or `desc` (descending).
    required: false
    schema:
      type: string
      enum: [asc, desc]
    example: asc

  - in: query
    name: isFavourite
    description: >
      Sort order for the `isFavourite` field.  
      Possible values: `asc` (ascending) or `desc` (descending).
    required: false
    schema:
      type: string
      enum: [asc, desc]
    example: desc

  - in: query
    name: contactType
    description: >
      Filter contacts by `contactType`.  
      Allowed values: `work`, `home`, or `personal`.
    required: false
    schema:
      type: string
      enum: [work, home, personal]
    example: work
responses: 
  '200':
    description: Successfully get students
    content:
      application/json:
        schema:
          type: object
          required:
            - status
            - message
            - data
          properties:
            status:
              type: integer
              example: 200
            message:
              type: string
              example: Successfully get students
            data:
              $ref: '../../components/schemas/contact.yaml'
  '401':
    $ref: ../../components/responses/401.yaml
  '500':
    $ref: ../../components/responses/500.yaml