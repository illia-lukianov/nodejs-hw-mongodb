description: Deleted student
content:
  application/json:
    schema:
      type: object
      required:
        - status
      properties:
        status:
          type: integer
          example: 204